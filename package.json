{
  "name": "straindard.com",
  "version": "0.1.0",
  "author": "gregory@straindard.com",
  "url": "https://straindard.com",
  "private": true,

  "engines": {
    "node": ">=10.11.0",
    "npm": ">=6.4.1"
  },

  "dependencies": {
    "normalize.css": "latest",
    "graphql": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-scripts": "latest",
    "pretty-error": "latest",
    "css-select": "latest"
  },

  "devDependencies": {
  },

  "optionalDependencies": {
    "nodemon": "latest"
  },

  "scripts": {
    "flow": "flow",
    "flow:check": "flow check",
    "start": "concurrently \"npm run graphql-server\" \"react-scripts start\"",
    "eject": "react-scripts eject"
  },

  "lint-staged": {
  }
}
